{
  "provider": {
    "kubernetes": {
      "host": "${data.terraform_remote_state.outputs.k8s_endpoint}",
      "token": "${data.aws_eks_cluster_auth.k8s.token}",
      "cluster_ca_certificate": "${base64decode(data.terraform_remote_state.outputs.k8s_ca_data)}"
    }
  }
}
