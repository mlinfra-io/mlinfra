${prefect_deplyoment_type}:
  nodeSelector: ${nodeSelector}
  tolerations: ${tolerations}
  affinity: ${affinity}
  resources: ${resources}
postgresql:
  primary:
    nodeSelector: ${nodeSelector}
    tolerations: ${tolerations}
    affinity: ${affinity}
