configuration: &configuration
  nodeSelector: ${nodeSelector}
  tolerations: ${tolerations}
  affinity: ${affinity}

<<: *configuration

pulsar:
  zookeeper:
    <<: *configuration
  bookkeeper:
    <<: *configuration
  autorecovery:
    <<: *configuration
  broker:
    <<: *configuration
  proxy:
    <<: *configuration
  pulsar_metadata:
    <<: *configuration
  pulsar_manager:
    <<: *configuration

etcd:
  <<: *configuration

mixCoordinator:
  <<: *configuration

rootCoordinator:
  <<: *configuration

indexCoordinator:
  <<: *configuration

queryCoordinator:
  <<: *configuration

dataCoordinator:
  <<: *configuration

proxy:
  <<: *configuration

minio:
  <<: *configuration

standalone:
  <<: *configuration
